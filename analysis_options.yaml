include: package:flutter_lints/flutter.yaml

# riverpod_lint 3.x uses the native analyzer plugin system (not custom_lint)
plugins:
  riverpod_lint: ^3.1.3

analyzer:
  exclude:
    - "**/*.g.dart"

linter:
  rules:
    lines_longer_than_80_chars: false
